apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "practiceChart.fullname" . }}-test-connection"
  labels:
    {{- include "practiceChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "practiceChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
