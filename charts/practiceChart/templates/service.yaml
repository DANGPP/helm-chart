apiVersion: v1
kind: Service
metadata:
  name: {{ include "practiceChart.fullname" . }}
  labels:
    {{- include "practiceChart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "practiceChart.selectorLabels" . | nindent 4 }}
